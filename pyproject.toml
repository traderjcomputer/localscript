[project]
name = "localscript"
version = "0.1.0"
description = "日常开发和管理小型 Python 脚本的项目文件夹"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
